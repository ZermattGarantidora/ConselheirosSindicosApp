# Registro de revisão local — CI remoto

**Data:** 2026-09-08
**Escopo:** workflow do GitHub Actions, isolamento de artefatos locais e documentação dos gates remotos
**Status:** APROVADO

## Diff revisado

- `.github/workflows/ci.yml`
- `.gitignore` e `eslint.config.mjs`, para impedir que worktrees e saídas locais contaminem a análise;
- documentação de CI, política de merge e tarefas de fundação/consulta documental.

## Verificações

- `pnpm run check` — aprovado: 154 testes, cobertura de statements 92,96%, branches 87,05%, functions 97,11% e lines 92,77%;
- `pnpm run test:e2e` — aprovado: 17 testes;
- `pnpm run build:web` — aprovado;
- `git diff --check` — aprovado.

## Parecer

Não foram encontrados achados P0 ou P1. O workflow usa permissões mínimas de leitura, instala com lockfile congelado, executa os gates de qualidade, E2E e build, e publica a cobertura como artefato. A integração Neon permanece fora do CI automático até existir um banco sintético dedicado e seus segredos configurados.

## Pendência explícita

A regra de proteção da `main` ainda precisa ser ativada no GitHub, exigindo pull request, aprovação, conversas resolvidas e o check `quality` verde, além de bloquear exclusão e force-push.
